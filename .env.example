# API base (devneya-api + GoTrue auth under /auth/*)
VITE_API_BASE_URL=https://api.devneya.com

# GoTrue anon/public key (Supabase-compatible client)
VITE_GOTRUE_ANON_KEY=

# Use MSW mocks instead of live API (default for local dev and CI e2e)
VITE_USE_MOCKS=true
